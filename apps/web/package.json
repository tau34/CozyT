{
  "name": "@cozyt/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:test": "vite --mode test",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "build:production": "tsc -p tsconfig.json --noEmit && vite build --mode production",
    "preview": "vite preview",
    "start:render": "vite preview --host 0.0.0.0 --port $PORT",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cozyt/core": "*",
    "@vitejs/plugin-react": "latest",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
